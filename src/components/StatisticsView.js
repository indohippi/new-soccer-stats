import React from 'react';

const StatisticsView = ({ players, actions }) => {
  return (
    <div>
      <h2>Statistics View</h2>
      {/* Display statistics for each player and for the team */}
    </div>
  );
};

export default StatisticsView;
